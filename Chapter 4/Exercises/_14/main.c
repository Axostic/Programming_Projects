#include <stdio.h>

int main(void)
{
	/*
	
	Use parentheses to show how C would interpret the expressions
	and their alike subexpressions.

	(a) a * b - c * d + e    -->    (((a * b) - (c * d)) + e)
	(b) a / b % c / d        -->    (((a / b) % c) / d)
	(c) - a - b + c - + d    -->    ((((-a) - b) + c) - (+d))
	(d) a * - b / c - d      -->    (((a * (-b)) / c) - d)

	*/
	
	return 0;
}