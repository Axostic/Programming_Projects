#include <stdio.h>

int main(void)
{
	// First problem: variable type is not specified for the second parameter (height).
	// --> Fix by adding "dobule" before height.
	// Second problem: "product" should not be a parameter.
	// --> Fix by declaring "double product;" inside the function body.

	return 0;
}