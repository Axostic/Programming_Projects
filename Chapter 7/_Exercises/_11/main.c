#include <stdio.h>

int main(void)
{
	/*
		Suppose i is an int variable, f is a float variable, and d is a double variable.
		What is the type of the expression i * f / d ?
		(i * (f / d))

		(f/d) evaluates to double.
		i * (f/d) evaluates to double.
	*/

	return 0;
}