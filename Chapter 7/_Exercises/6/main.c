#include <stdio.h>

int main(void)
{
	/* which is large enough to hold: Char, short, int, or long?
		Days in a month		--	30, so char.
		Days in a year		--	365, so short.
		Minutes in a day	--	1440, so short.
		Seconds in a day	--	86400, so long (Int is suitable as well if on a 32-bit+ machine)
	*/

	return 0;
}