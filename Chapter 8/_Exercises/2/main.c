#include <stdio.h>

int main(void)
{
    // To use a digit (in character form) as an array subscript, use
    // a[digit - '0']
    // The characters evaluate to integers; consequently, the subscript also
    // evaluates to an integer.
    // Like the book states in the case of letter subscripts, this only
    // works with character sets that have a proceding order (0,1,2,3....9)

    return 0;
}
